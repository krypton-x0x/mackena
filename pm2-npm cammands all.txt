🟢 NPM команды
Основные команды:
npm access — Управление доступом к приватным пакетам.

npm adduser — Регистрация нового пользователя.

npm audit — Проверка пакетов на наличие уязвимостей.

npm bin — Путь до бинарных файлов пакетов.

npm bugs — Ссылка на страницу багов пакета.

npm cache — Управление кэшем npm.

npm ci — Установка зависимостей из package-lock.json (быстрее и безопаснее, чем install).

npm completion — Настройка автодополнения команд в терминале.

npm config — Просмотр и настройка конфигурации npm.

npm dedupe — Оптимизация дублирующихся зависимостей.

npm deprecate — Пометить версию пакета устаревшей.

npm dist-tag — Управление тегами версий пакета.

npm doctor — Диагностика среды разработки и исправление ошибок.

npm docs — Открыть документацию по пакету в браузере.

npm edit — Открывает папку пакета для редактирования.

npm exec — Запуск команд из бинарников установленных пакетов (аналог npx).

npm explain — Показать, почему установлен конкретный пакет.

npm explore — Войти в папку установленного пакета.

npm find-dupes — Поиск дублирующихся пакетов.

npm fund — Информация о финансовой поддержке пакетов.

npm help — Помощь по командам npm.

npm hook — Управление веб-хуками пакетов.

npm init — Создание файла package.json.

npm install — Установка пакетов и зависимостей.

npm link — Создание ссылки на локальный пакет.

npm login — Вход в аккаунт npm.

npm logout — Выход из аккаунта npm.

npm ls — Просмотр дерева зависимостей.

npm org — Управление организацией npm.

npm outdated — Проверка устаревших пакетов.

npm owner — Управление владельцами пакета.

npm pack — Упаковка пакета в архив.

npm ping — Проверка связи с npm-сервером.

npm pkg — Просмотр и редактирование package.json.

npm prefix — Путь до корня текущего проекта.

npm profile — Управление профилем npm.

npm prune — Удаление неиспользуемых пакетов.

npm publish — Публикация пакета.

npm rebuild — Пересборка пакетов с нативными расширениями.

npm repo — Открывает репозиторий пакета в браузере.

npm restart — Перезапуск скрипта из package.json.

npm root — Путь до папки node_modules.

npm run — Выполнение скрипта из package.json.

npm search — Поиск пакетов в npm.

npm shrinkwrap — Создание файла npm-shrinkwrap.json для блокировки зависимостей.

npm star — Поставить звезду пакету.

npm stars — Просмотр отмеченных пакетов.

npm start — Запуск скрипта "start" из package.json.

npm stop — Остановка скрипта "stop" из package.json.

npm team — Управление командами npm.

npm test — Запуск скрипта "test" из package.json.

npm token — Управление токенами доступа.

npm uninstall — Удаление пакета.

npm unpublish — Удаление пакета из npm-реестра.

npm unstar — Убрать звезду с пакета.

npm update — Обновление пакетов.

npm version — Изменение версии пакета.

npm view — Просмотр информации о пакете.

npm whoami — Текущий пользователь npm.

🔵 PM2 команды
Основные команды:
pm2 completion — Включить автодополнение команд pm2.

pm2 delete <app> — Остановить и удалить процесс.

pm2 deploy <env> — Деплой приложения согласно конфигурации.

pm2 describe <app> — Подробная информация о процессе.

pm2 ecosystem — Создание конфигурационного файла экосистемы для деплоя.

pm2 flush — Очистка логов всех процессов.

pm2 info <app> — Информация о конкретном процессе.

pm2 init — Создание начального файла конфигурации (ecosystem.config.js).

pm2 install <module> — Установка модуля pm2.

pm2 link — Связывание локального процесса с PM2 Cloud.

pm2 list — Список всех активных процессов.

pm2 logrotate — Настройка ротации логов.

pm2 logs [app] — Просмотр логов процессов в реальном времени.

pm2 monit — Мониторинг состояния и производительности процессов.

pm2 plus — Подключение к PM2 Plus (платное облачное решение).

pm2 reload <app> — Перезагрузка процесса с нулевым простоем.

pm2 reset <app> — Сброс счетчиков ошибок и рестартов.

pm2 restart <app> — Перезапуск процесса.

pm2 resurrect — Восстановление всех процессов после перезапуска системы.

pm2 save — Сохранение текущего состояния процессов.

pm2 scale <app> <num> — Масштабирование количества экземпляров приложения.

pm2 show <app> — Подробная информация о процессе (аналогично describe).

pm2 start <app|script> — Запуск приложения или скрипта.

pm2 startup — Генерация startup-скрипта для автозапуска при перезагрузке ОС.

pm2 status — Информация о состоянии процессов (аналогично list).

pm2 stop <app|all> — Остановка процессов.

pm2 trigger <app> <action> — Выполнение пользовательских действий над процессом.

pm2 uninstall <module> — Удаление модуля pm2.

pm2 unlink — Отвязка от PM2 Cloud.

pm2 update — Обновление самого pm2.

pm2 version — Просмотр версии pm2.

